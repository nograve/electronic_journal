import 'package:flutter/material.dart';

class HomePageSettingsTab extends StatelessWidget {
  const HomePageSettingsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('created by @nograve');
  }
}
